
<div class="jumbotron">
    <h1>Book Holidays</h1>
    <% if $has_auth_provider %>
        <p>Welcome to the holiday application, a place to manage employee holidays.</p>
        <p><a class="btn btn-primary btn-lg" role="button" href="/auth/ldap">Sign in</a></p>
    <% else %>
        <p>Note for developers: you need to set up an authentication provider before you can use the app.</p>
        <ol>
          <li>To set up for active directory run <code>rake configuration:generate_ldap_config</code>.</li>
          <li>The rake will generate a template file at <code>~/config/ldap.yml</code>.</li>
          <li>Edit the template file, replacing the values with your domain settings.</li>
          <li>Restart the server.</li>
        </ol>
<% end %>
</div>